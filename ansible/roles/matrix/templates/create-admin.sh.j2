#!/bin/bash
# Create Matrix admin user
# Generated by Ansible - run this manually if the admin user wasn't created automatically

docker exec -it matrix-synapse-1 register_new_matrix_user \
    -c /data/homeserver.yaml http://localhost:8008 \
    --admin \
    -u {{ matrix_admin_user }} \
    -p {{ matrix_admin_password }}
