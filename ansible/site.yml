---
# Combined playbook — runs both install and deploy in one shot.
# Only works on a fresh server before SSH hardening disables root login.
- import_playbook: install.yml
- import_playbook: deploy.yml
